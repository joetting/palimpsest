[package]
name = "voxel_engine"
version = "0.1.0"
edition = "2021"

[dependencies]
rayon = "1.8.0"
serde = { version = "1.0", features = ["derive"] }
rand = "0.8"
image = "0.24"

[profile.release]
opt-level = 3
lto = "thin"
